name: init

on: 

# Allows you to run this workflow manually from the Actions tab
   workflow_dispatch:

jobs:

  call-workflow-A:
    uses: pankaj-iit/NodeJs-MS-Fastify-Template/.github/workflows/createTemplate.yml@main
    with:
      username: Houssem
